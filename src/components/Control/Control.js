import './Control.css';
import React from 'react';

const Control = ({ children }) => {
    return <div className='control'>
                    {children}
        </div>
    
};

export default Control;
